import {createTheme} from "@mui/material"

const theme = createTheme({
    palette: {
        mode: 'dark',
        background: {
            default: '#22303c',
        }
    }
})

export default theme;